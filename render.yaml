# Render service definition template for the static frontend.
# Update service name, region, and environment variables as needed.
services:
  - type: web
    name: risklens-frontend
    env: docker
    plan: free
    dockerfilePath: Dockerfile.frontend
    buildCommand: ""
    startCommand: ""
    envVars: []
